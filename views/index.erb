<h3>There are currently <a href="/character"><%= @characters.count - 1%> characters</a> in the database <% if @characters.count == 1 %>:(<% end %></h3>

<h3>There are currently <a href="/item"><%= @items.count %> items</a> in the database <% if @items.count == 0 %>:(<% end %></h3>

<h3>There are currently <a href="/race"><%= @races.count %> races</a> in the database</h3>

<h3>There are currently <a href="/role"><%= @roles.count %> roles</a> in the database</h3>
