<h1>Item <%= @item['item_name'] %> is about to be deleted!</h1>


<h2>Are you sure this is what you want?</h2>


<form action="/item/<%= @item['id'] %>/delete" method="post">
  <button type="submit">Yes</button>
</form>
<form action="/item/<%= @item['id'] %>">
  <button type="submit">No, back to item</button>
</form>
